import { StyleSheet, Text, View } from 'react-native';

type Props = {};

const Home = (props: Props) => {
	return (
		<View>
			<Text>home</Text>
		</View>
	);
};

export default Home;

const styles = StyleSheet.create({});
